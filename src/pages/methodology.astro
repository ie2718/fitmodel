---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';
import { absoluteUrl, isoDate, siteUrl } from '../lib/meta';

const evidence = await getCollection('evidence');
const metrics = await getCollection('metrics');
const site = Astro.site!;
const dataAsOf = evidence.reduce(
  (max, e) => (e.data.retrieved_at > max ? e.data.retrieved_at : max),
  evidence[0]?.data.retrieved_at ?? new Date(),
);
const schema = [
  {
    '@context': 'https://schema.org',
    '@type': 'Dataset',
    name: 'FitModel 证据库与评分方法',
    description:
      'FitModel 的证据库与评分方法论公示：来源白名单、指标注册制、合理域与时效校验、百分位归一化、场景加权评分、三档推荐规则。',
    url: absoluteUrl('/methodology/', site),
    inLanguage: 'zh-CN',
    isAccessibleForFree: true,
    license: 'https://creativecommons.org/licenses/by/4.0/',
    creator: { '@type': 'Organization', name: 'FitModel', url: absoluteUrl('/', site) },
    variableMeasured: metrics.map((m) => m.data.label),
    citation: [...new Set(evidence.map((e) => e.data.source_url))],
    dateModified: isoDate(dataAsOf),
  },
];
---

<Base
  title="方法论 — FitModel · 选型指南"
  description="FitModel 如何从公开数据生成推荐：采集来源、校验规则、归一化与评分算法、置信度体系，完整公示。"
  image="/og/methodology.png"
  schema={schema}
>
  <article>
    <header class="page-header">
      <h1>推荐是怎么算出来的</h1>
      <p class="summary">
        FitModel 的每一条推荐结论都由可溯源的公开数据经确定性算法计算生成——本页公示完整流程。
      </p>
    </header>

    <section>
      <h2 class="section-title">目标与边界</h2>
      <p class="section-sub">
        声明局限比展示公式更重要。以下是本站**做不到**和**不打算做**的事：
      </p>
      <ul class="plain">
        <li>不做主观评测：没有「我觉得 XX 写得最好」这种内容。人类偏好的数据来自 arena.ai 的盲测投票，不是我们的编辑。</li>
        <li>不做私有基准：所有数据都来自公开渠道（官方文档、独立基准站），可独立验证。</li>
        <li>不保证绝对正确：数据会过期、采集可能有遗漏、来源可能有误。我们用置信度分数诚实地表达这种不确定。</li>
        <li>不替代实测：排名反映的是统计倾向，个人体验因任务、提示方式、产品形态而异。排名是起点，不是终点。</li>
      </ul>
    </section>

    <section>
      <h2 class="section-title">第一步：数据采集</h2>

      <p><strong>来源白名单</strong> — 只从以下渠道取数，禁止从自媒体转述或论坛帖子取值：</p>
      <ul class="plain">
        <li><strong>官方一手</strong>（tier 1）：各厂商 API 定价页、模型文档、发布博客。用于价格、上下文窗口等技术规格。</li>
        <li><strong>独立基准</strong>（tier 2）：<a href="https://arena.ai/leaderboard" target="_blank" rel="noopener">arena.ai</a>（原 LMArena）人类盲测投票、<a href="https://swebench.com" target="_blank" rel="noopener">SWE-bench</a> 真实 issue 修复率、<a href="https://artificialanalysis.ai/models" target="_blank" rel="noopener">Artificial Analysis</a> 自研评测智能指数（AA-Briefcase、τ³-Banking、Terminal-Bench 等，含 held-out 防污染）。用于能力排名。</li>
        <li><strong>聚合二手</strong>（tier 3）：聚合站转载的内容（如 Artificial Analysis 的价格 / 速度页转载官方价）。仅作交叉参考，可靠度权重减半。注意：AA 的<strong>自研评测智能指数</strong>不算转载，属上方 tier 2。</li>
        <li><strong>社区口碑</strong>（tier 3）：公开社区用户讨论按预置 rubric 转译的净信号（<a href="https://www.reddit.com/r/LocalLLaMA/" target="_blank" rel="noopener">Reddit</a> / <a href="https://news.ycombinator.com/" target="_blank" rel="noopener">HN</a> / 知乎 / V2EX 等）。只记正/负讨论串计数与原文引用，<strong>禁止从论坛帖取任何能力数值</strong>；规则见下方「社区口碑」节。</li>
      </ul>

      <p style="margin-top:18px"><strong>采集规则</strong></p>
      <ul class="plain">
        <li>每条证据必须包含：指标 id、模型变体名、原始值、来源名、可回查 URL、采集日期。</li>
        <li>不确定的值一律标 <code class="num">verified: false</code>，允许展示但不允许参与评分。</li>
        <li>JS 渲染的页面（如智谱定价页）标注「需二次采集」，用浏览器 agent 或人工获取。</li>
      </ul>
    </section>

    <section>
      <h2 class="section-title">第二步：校验</h2>

      <p><strong>指标注册制</strong> — 所有可入分的指标必须先在 <code class="num">metrics.yaml</code> 注册：</p>
      <div class="formula">
指标 id：arena_elo_overall
合理域：[800, 1700]       ← 超出此域的数据被标记为「域外」，仅展示不入分
时效窗口：90 天          ← 超过 90 天未更新的证据标记为「过期」，仅展示不入分
方向：higher              ← Elo 越高越好（反之为 lower）
来源层级：tier 2          ← 该指标只接受独立基准来源，tier 1 官方文档不入此指标</div>

      <p><strong>自动校验</strong> — 构建时（<code class="num">npm run build</code>）执行：</p>
      <ul class="plain">
        <li><strong>注册检查</strong>：evidence.yaml 中引用的 metric id 必须在 metrics.yaml 中注册，否则构建直接失败。</li>
        <li><strong>合理域检查</strong>：值落在 plausible 区间外 → 标记「域外」，降级为仅展示。</li>
        <li><strong>时效检查</strong>：retrieved_at 超过 freshness_days → 标记「过期」，降级为仅展示。</li>
        <li><strong>冲突检测</strong>：同一 (指标, 模型变体) 同日多条证据值分歧 >10% → 取同日中位数入分，并降置信度。</li>
      </ul>
    </section>

    <section>
      <h2 class="section-title">第三步：归一化（双轨制）</h2>

      <p>通过校验的证据进入同组归一化——**同指标组内比较，不跨指标直接比大小**。按指标类型分两轨：</p>

      <p><strong>轨道一：序数型指标（Arena 分类目名次）→ 固定前沿参考系 Beta 百分位</strong>。名次只有先后、没有量级。参考系 R 与采集政策对齐（前沿 50 名），百分位按「名次 / 参考系」而非「名次 / 已采集条数」——采集范围扩大时分数不再漂移：</p>
      <div class="formula">参考系大小 R（如 50），某变体名次 r（1 = 最好）：

  百分位   pct = (R + 1 − r) / (R + 1) × 100     ← 「#3 / 50」永远 = 94.1
  标准误   se  = max(4, 100 × √( r(R+1−r) / ((R+1)²(R+2)) ))
  r > R（超出参考系）→ 贴地保底 pct ≥ 1

  未定义 R 的名次指标按已采集条数回退计算。
  特例：组内仅 1 条 → 50 ± 28.9（等于没有信息，由置信度表达）</div>

      <p><strong>轨道二：区间型指标（Elo、解决率、价格等有量纲的值）→ z-score → 正态 CDF</strong>。名次百分位会把「1 分之差」放大成几个百分位（例：SWE-bench 榜首集群 76.8% vs 75.8%，名次分差悬殊而实际几乎并列），z-score 保留量级——分差越大百分位差越大：</p>
      <div class="formula">z = (x − 组内均值) / σ_eff      （价格先取对数再算 z，跨数量级比较）
  σ_eff = √(组内σ² + 测量噪声²)    ← 拥挤前沿保护（2026-09-12）：
  arena Elo 组内 σ 仅 ≈8，小于 LMArena 单模型 CI（±10–30），
  不膨胀会把 ±5 Elo 的纯噪声放大成 25+ 百分位差（实测：
  同一信号两种编码读数相差 50 百分位——Elo 27 vs 名次 76）
  方向为 lower（越低越好）时 z 取反
  z 温缩到 ±2.6，防离群点独占 0/100

  百分位   pct = Φ(z) × 100                 ← Φ = 标准正态 CDF
  标准误   se  = 100 × φ(z) × √((1+z²/2)/n)  ← delta 法

  n < 4 时 z 不稳定，回退轨道一。</div>

      <p>选型依据见<a href="https://github.com/ie2718/fitmodel/blob/main/docs/research-2026-09.md" target="_blank" rel="noopener">行业调研</a>：percentile 分布无关、抗离群（适合名次）；z-score 跨基准可比性好、保留量级（适合区间值）；两者都天然携带不确定性，供第四步合成误差界。</p>
    </section>

    <section>
      <h2 class="section-title">第四步：维度聚合与场景评分</h2>

      <p><strong>可靠度加权聚合</strong>——同一维度下有多条指标证据时，越可信、越新鲜、越无争议的证据话语权越大；公示的指标权重在可靠度之外提供第二重话语权：</p>
      <div class="formula">证据权重  w = 指标权重 × 来源层级 × 时效衰减 × 一致性

  指标权重   metrics.yaml 公示，缺省 1.0
             （AA 智能指数 = 2.0，2026-09-08 经人工确认上调）
  来源层级   tier 1 = 1.0 · tier 2 = 0.85 · tier 3 = 0.5
  时效衰减   1 − 0.5 × (数据年龄 / 时效窗口)     ← 窗口内线性 1.0 → 0.5
  一致性     存在同日冲突证据 × 0.85

  维度分 = Σ(w × pct) / Σw
  维度误差 = √( 指标内误差² + 指标间分歧² )，下限 6 分</div>

      <p><strong>缺席先验与一致性折减（2026-09-12 新增）</strong>——修复「旧代模型靠类目名次压过现役旗舰」的结构性偏差：</p>
      <div class="formula">① 缺席先验   指标注册 absence_prior 时（当前仅 AA 智能指数 30 ± 24）
             实体在该维度有其他实测、唯独缺该指标 → 注入先验事实参与聚合
             依据：AA 为现役精选口径，官方下架被替代代际（GPT-5.6 先例）
             缺席本身即「非现役」证据，期望低于在榜中位且高度不确定

② 覆盖滞后折减  名次型事实低于同一实体其余实测共识（加权中位数）40 分以上
             且其余实测 ≥4 条 → 可靠度 ×0.5，依据列标注「类目覆盖存疑」
             依据：新旗舰进入类目榜滞后（同 Text 总榜曾缺 GPT-6），
             覆盖滞后只会表现为名次异常低，故只单向向下折减

③ 纯名次维度收缩  非现役实体（触发①）的维度若全部由名次型证据构成
             → 维度分与缺席先验 50/50 收缩，先验不确定性并入误差界
             含区间型实测（SWE-bench / AA / 规格值）的维度不受影响</div>

      <p><strong>场景适配分（Fit Score）</strong>——每个场景公示一组维度权重（和 = 1）：</p>
      <div class="formula">例：「IDE 内日常编码」  coding 65% · general 25% · long_context 10%

  Fit = Σ(维度权重 × 维度分) / Σ(维度权重)

  维度缺数据时（经验贝叶斯收缩，用「知情先验」）：
  · 该维度全站有数据、此实体没有 → 按知情先验收缩，±28.9 计入误差界
    先验 = 50 + (自身实测均值 − 50) × 证据可靠度(来源层级×样本量)，夹在 [30, 70]
    （已知很强的模型不被「缺数据」拖成平庸；证据越薄越向中位回落，单基准模型照样上不了天）
  · 该维度全站都无数据（缺口）→ 不参与任何实体的评分，只降覆盖率</div>

      <p><strong>误差界（90% CI）</strong>沿归一化 → 维度 → 场景逐层合成，展示为「分数 ±区间」：</p>
      <div class="formula">se_fit = √( Σ(权重 × se)² ) / Σ权重
  90% 区间 = Fit ± 1.645 × se_fit

  区间重叠的相邻名次 → 标记「≈ 并列」。
  分数接近但我们分不出高下时，诚实地说分不出。</div>

      <p><strong>置信度（Confidence）</strong>由五个因子相乘——Glicko 式时效处理：分数不因数据变旧而缩水，确定性降级：</p>
      <div class="formula">Confidence = Coverage × SourceFactor × SampleFactor × FreshFactor × AgreeFactor

  Coverage     = 已实测维度权重 / 总权重（缺口直接降低确定性）
  SourceFactor = 证据来源层级加权均值（tier 1 = 1.0 / 2 = 0.85 / 3 = 0.5）
  SampleFactor = min(1, 组内比较对象数 / 10)（同组样本不足 → 降信心）
  FreshFactor  = 1 − 0.2 × (数据年龄 / 时效窗口)
  AgreeFactor  = 1.0（正常）/ 0.9（存在同日冲突）

  <span class="op">→ 0.0–1.0，≥70% 高 / 40–70% 中 / <40% 低</span></div>

      <p><strong>性价比分（Value Score）</strong>：ratio 口径（行业实践：ratio 比归一化相除更稳）。只考虑有真实能力证据（非价格）的变体，且 Fit 取实测适配分（不含先验收缩）——否则纯价格证据的变体会以先验分 × 价格上限齐满，档位失去信息量。</p>
      <div class="formula">Value = Fit_measured × min(2, price_median / blended_price)

  blended_price = input_price + output_price（标准档，不含缓存）
  price_median  = 同组混合价中位数
  <span class="op">→ 适配分相同但价格更低 → 性价比更高；上限 2× 防极端低价虚高</span></div>

      <p><strong>综合能力指数</strong>（榜单页）：以公示权重 general 45% · coding 35% · writing 10% · long_context 10% 对全部变体计算 Fit（2026-09-12 起）。仿 Artificial Analysis 的做法——权重必须公示，改动必须在更新日志留痕。<strong>权重与证据厚度相称</strong>：通用维度有 5+ 信号（含 AA 能力锚）且 arena 家族按同源去相关折价（Elo/总榜名次/难题/指令各 0.5，家族预算 = AA 权重，通用维度内偏好:能力 = 50:50）；编码有类目+WebDev+SWE 三种仪器；写作是唯一没有任何能力锚证据（AA/SWE/规格都不覆盖）、仅靠单一偏好类目的维度，权重降为 0.1。长上下文维度由 arena 长查询类目驱动（用户偏好信号）。arena 未收录的新模型（如 GPT-6）经 AA 先行入榜、低置信度展示，收录后自动增强；被 AA 现役榜下架的旧代变体（如 opus-4.x、GPT-5.6 系）按非现役收缩，不再以历史类目名次冒充现役能力（2026-09-12，响应「综合榜不客观」反馈，波及分析与触发清单见更新日志）。</p>
    </section>

    <section>
      <h2 class="section-title">第五步：三档推荐规则</h2>

      <p>三档推荐是确定性规则，无人工挑选、无黑箱：</p>
      <ul class="plain">
        <li><strong>TOP（数据上最强）</strong>：该场景排名表第 1 名。</li>
        <li><strong>VALUE（性价比）</strong>：性价比分最高者（前提：与 TOP 不是同一个实体，否则不重复展示）。</li>
        <li><strong>FREE（免费首选）</strong>：在所有有免费额度的产品中，先按品类匹配场景（写作场景不推荐 IDE，编码场景不推荐聊天 App），再优先国内直接可用者（存在直连候选时排除访问受限产品），最后取其底座模型在当前场景的适配分最高者。免费档是<strong>产品层推荐</strong>——展示为「产品 + 底座模型家族」，不承诺免费档可用的具体变体（产品方未公开时我们不做猜测）。</li>
      </ul>
      <p class="section-sub">
        三个推荐各有适用条件——并非所有人都需要「最强」，这恰恰是三档存在的理由。
      </p>
    </section>

    <section>
      <h2 class="section-title">与行业方法的对照</h2>
      <p class="section-sub">
        本站算法不是发明，而是把公认做法装进「证据驱动静态站」的约束里（逐条依据见
        <a href="https://github.com/ie2718/fitmodel/blob/main/docs/research-2026-09.md" target="_blank" rel="noopener">docs/research-2026-09.md</a>）：
      </p>
      <ul class="plain">
        <li><strong>LMArena</strong>：Bradley-Terry + bootstrap 置信区间 → 我们没有对局数据，用解析误差界（名次 Beta 后验 / 区间值 z-score delta 法）达到同样的「分数必须带区间」。</li>
        <li><strong>Artificial Analysis</strong>：公示权重的加权算术平均、样本量披露 → 综合指数权重公示，组内样本量进置信度。</li>
        <li><strong>HELM</strong>：反对单一总分、保留分场景可见性 → 综合指数之外，每个场景独立权重独立排名。</li>
        <li><strong>Glicko</strong>：分数不缩水、确定性随时间降级 → FreshFactor 只降置信度不改分数，过期由硬门禁剔除。</li>
        <li><strong>Vellum / SWE-bench</strong>：排除饱和基准、口径版本化留痕 → 指标注册制 + changelog 口径记录，饱和基准的退出规则在指标注册审批中执行。</li>
      </ul>
    </section>

    <section>
      <h2 class="section-title">Harness 榜（产品层）</h2>
      <p>
        榜单页的「Harness」tab 是<strong>产品层榜单</strong>：AI 编程工具与 Agent 脚手架（TRAE、Rovo Dev、Warp、OpenHands 等）在
        SWE-bench Verified 上的公开最好成绩。口径与模型榜不同——模型榜用 bash-only（mini-SWE-agent）统一环境隔离模型能力，
        Harness 榜是「工具 × 其搭配模型」的组合成绩，<strong>横向比较含模型因素</strong>，搭配模型逐行明示。每工具取公开最优运行。
        未公开提交的知名产品（Cursor、Claude Code、GitHub Copilot 等）单列展示产品属性、不参与排名。Harness 成绩不进入模型能力评分与场景推荐。
      </p>
    </section>

    <section>
      <h2 class="section-title">AA 智能指数（外部榜单）</h2>
      <p>
        榜单页的「AA 智能指数」tab 镜像 <a href="https://artificialanalysis.ai/models" target="_blank" rel="noopener">Artificial Analysis</a>
        的 Intelligence Index 原始榜（当前 v4.3：AA-Briefcase、GDPval-AA v2、AutomationBench-AA、Terminal-Bench v4.0、SciCode、
        Humanity's Last Exam、GDP.pdf、CritPt、AA-Omniscience、AA-LCR v1.1 共 10 项评测加权合成，含私有 held-out 防污染）。
        它是唯一一个<strong>进入能力评分且带显式指标权重（2×）</strong>的外部榜单：独立自研基准与 arena 人类盲测方法论正交，
        两者互为制衡；同时承担 arena 未收录新模型的先行入榜通道，并作为<strong>现役前沿锚</strong>——未上榜变体按非现役
        缺席先验收缩（见第四步①③）。版本演进（v4.2→v4.3）数值不可比，
        同变体旧值由 latest 聚合自动替换并在证据 notes 留痕。
      </p>
    </section>

    <section>
      <h2 class="section-title">社区口碑（用户真实反馈）</h2>
      <p>
        2026-09-12 起接入的第七类来源：Reddit / Hacker News / 知乎 / V2EX / linux.do 等
        公开社区的用户讨论。它<strong>不是</strong>把论坛意见打成分数——而是把定性反馈按预置规则转译为可回查的净信号
        （指标 <code class="num">community_signal</code>，tier 3 · weight 1，约 6% 综合话语权）。
        它存在的意义是捕捉<strong>仪器分数与真实体验的背离</strong>：基准测得好但用户骂、或分数平平但用户称赞的模型，
        由这个信号显式修正。
      </p>
      <p><strong>计数 rubric（预置公示，采集前生效）</strong>：</p>
      <ul class="plain">
        <li><strong>净信号 = 正面讨论串数 − 负面讨论串数</strong>；计数单位是独立讨论串（不同帖子 / 不同社区）。</li>
        <li>只计<strong>针对模型能力与使用体验的具体可归因陈述</strong>（「编码两行 PR 大范围重写」「5 小时额度 20 分钟烧完」）；泛泛站队不计。</li>
        <li><strong>混合倾向串不计</strong>（同一串内正负都有且无明确主倾向）。</li>
        <li>定价与成本体验属真实使用反馈（<strong>计入</strong>）；自托管硬件门槛属部署条件（<strong>不计</strong>）；媒体评测与厂商稿件不算用户反馈。</li>
        <li>每模型 ≥2 个独立讨论串才入册；<strong>|净信号| &lt; 2 视为信号不足（计数噪声 ±1.5 之内），仅在「用户口碑」tab 展示、不入分</strong>——否则中性口碑会稀释高分模型，而无口碑数据的模型反而不被稀释；无讨论数据的变体同样不参与（不罚分）。</li>
      </ul>
      <p>
        <strong>已知偏差（诚实声明）</strong>：自选样（发帖人≠全体用户）、语言偏差（英文社区为主，辅以中文社区）、无法盲测、
        情绪易被发布会周期放大。因此它只拿 tier 3 / weight 1 的修正信号权重，正负计数与原文引用全部进证据库、
        逐条可回查原帖（榜单页「用户口碑」tab）。每轮周同步复核一次，口碑随时间衰减（时效窗口 30 天）。
      </p>
    </section>

    <section>
      <h2 class="section-title">基准污染与饱和登记表</h2>
      <p>
        评测文献的核心共识之一是「基准会饱和、会泄漏、会被博弈」（<a href="https://arxiv.org/abs/2502.17521" target="_blank" rel="noopener">污染综述</a>、<a href="https://arxiv.org/abs/2504.20879" target="_blank" rel="noopener">The Leaderboard Illusion</a>、
        <a href="https://arxiv.org/abs/2406.19314" target="_blank" rel="noopener">LiveBench</a>）。本站对各基准的已知状态与对策公示如下，随采集轮次更新：
      </p>
      <div class="table-wrap">
        <table>
          <thead>
            <tr><th>基准 / 信号</th><th>已知问题</th><th>本站对策</th></tr>
          </thead>
          <tbody>
            <tr>
              <td>arena.ai 盲测</td><td>风格 / 位置噪声；私有变体测试与不对称数据访问（Leaderboard Illusion）；类目覆盖滞后于新旗舰</td>
              <td>不单一依赖（多仪器制衡）；同源家族折价（0.5×4）；Elo 噪声膨胀 ±15；类目覆盖滞后折减 ×0.5</td>
            </tr>
            <tr>
              <td>SWE-bench Verified</td><td>饱和 + SOTA 模型被指「记忆而非推理」（SWE-bench Illusion）；官方最新运行 2026-02 早于当前旗舰</td>
              <td>bash-only 统一口径 + 运行日期逐条留痕；旗舰缺此证据不视为弱（知情先验），警示语公示于编码维度</td>
            </tr>
            <tr>
              <td>AA 智能指数</td><td>版本间数值不可比（v4.2→v4.3 整体下移）；榜单为现役精选（会下架旧代）</td>
              <td>版本口径留痕 + latest 聚合替换；现役锚 + 缺席先验（非现役 30±24）</td>
            </tr>
            <tr>
              <td>社区口碑</td><td>无法盲测、自选样、语言偏差、发布周期情绪放大</td><td>tier 3 / weight 1（约 6%）修正信号；rubric 预置；逐条回查原帖；时效 30 天</td>
            </tr>
            <tr>
              <td>官方定价 / 规格</td><td>促销价有期限；动态渲染页易漏更新</td><td>tier 1 一手来源；到期前一周复核；JS 页标注需二次采集</td>
            </tr>
          </tbody>
        </table>
      </div>
      <p class="section-sub" style="margin-top:14px">
        本站评分引擎的十条测量原则（区间必备、同源折价、收缩先验、精度纪律等）及其 100 篇文献依据见
        <a href="https://github.com/ie2718/fitmodel/blob/main/docs/research-papers-2026-09.md" target="_blank" rel="noopener">docs/research-papers-2026-09.md</a>（P1–P10）；
        跨仪器一致性（Spearman）随每周巡检输出在 <code class="num">npm run sync</code> 报告中，低相关仪器对提请人工复核。
      </p>
    </section>

    <section>
      <h2 class="section-title">Agent 数据通道</h2>
      <p>
        全部结论都有机器可读版本：<a href={siteUrl('/api/')}>静态 JSON API（/api/v1/*）</a>与
        <a href={siteUrl('/skill.md')}>SKILL.md（/skill.md）</a>。端点构建时生成、URL 稳定、CORS 全开、
        自描述（schema_version / generated_at / data_as_of 内嵌），其他 Agent 接入后即可按场景查询推荐。
        llms.txt / llms-full.txt 保留作为索引入口。
      </p>
    </section>

    <section>
      <h2 class="section-title">局限与改进方向</h2>
      <ul class="plain">
        <li><strong>证据覆盖不足</strong>：部分模型只有单一基准数据。经验贝叶斯收缩会让它们在分数与误差界上付出代价（向中位收缩、区间变宽），而不是消失或虚高——但根本解法仍是补齐各基准数据。豆包、混元、文心等仍缺独立基准排名。</li>
        <li><strong>解析误差界是近似</strong>：把排名不确定性建模为 Beta 后验并逐层合成，比 LMArena 的 bootstrap 粗糙；在证据极稀疏时区间可能仍偏窄。</li>
        <li><strong>产品层能力未量化</strong>：模型排名 ≠ 产品体验。IDE 索引质量、ASR 准确率、排版渲染等能力目前无法量化，导致会议纪要、图像生成等场景暂不开评分。</li>
        <li><strong>维度权重是判断</strong>：综合指数的 40/30/20/10 与各场景权重都是人为判断，我们选择公示而非掩饰；调整必须留痕。</li>
        <li><strong>国内可用性标记是手动的</strong>：网络环境变化频繁，标记可能滞后。建议最终使用前以实测为准。</li>
      </ul>
    </section>

    <section>
      <h2 class="section-title">算法实现</h2>
      <p class="section-sub">
        评分引擎是一个纯函数库（<code class="num">src/lib/scoring.ts</code>），构建时运行，无副作用。数据文件位于 <code class="num">src/data/</code>。
      </p>
      <ul class="evidence-list">
        <li>
          <span class="mono">metrics.yaml</span>
          <span>指标注册表（合理域、时效窗口、方向、来源层级）</span>
        </li>
        <li>
          <span class="mono">evidence.yaml</span>
          <span>证据库（150 条可溯源记录，含来源 URL 与采集日期）</span>
        </li>
        <li>
          <span class="mono">scoring.ts</span>
          <span>评分引擎：校验 → 去重/冲突取中位 → 双轨归一化（名次 Beta / 区间值 z-score）→ 可靠度加权聚合 → 适配分/误差界/置信度/性价比</span>
        </li>
        <li>
          <span class="mono">board.ts / api.ts</span>
          <span>综合指数口径与 Agent JSON API 的共享装配层</span>
        </li>
        <li>
          <span class="mono">scenarios/*.md</span>
          <span>场景页：编辑只定义维度权重和非排名性建议，排名由引擎计算</span>
        </li>
        <li>
          <span class="mono">scripts/sync.mjs</span>
          <span>一键同步：鲜度巡检 → 来源变更检测 → 采集任务卡 → 排名影响对比（npm run sync）</span>
        </li>
        <li>
          <span class="mono">docs/research-2026-09.md</span>
          <span>行业调研与算法选型依据（LMArena / AA / HELM / Glicko 等）</span>
        </li>
      </ul>
    </section>
  </article>
</Base>
